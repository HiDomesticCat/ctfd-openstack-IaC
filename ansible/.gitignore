# 主機清單（含真實 IP 與 SSH 金鑰路徑）
inventory/hosts.ini

# Ansible Vault 已加密的實際 vault 檔案
group_vars/all/vault.yml

# 環境特定設定（UUID、IP、flag 等，因環境而異，不 commit）
group_vars/all/challenge.yml
# 由 ctfd tofu 自動產生（network_id、image_id）
group_vars/all/challenge_ids.yml
# 由 chell tofu 自動產生（worker IPs）
group_vars/all/k3s_ids.yml

# 其他敏感資訊
*.retry
